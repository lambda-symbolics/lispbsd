#!/bin/sh
# LispBSD guest configuration. Conventional daemons stay off; the
# console is the live Lisp world.
. /etc/defaults/rc.conf
rc_configured=YES
hostname=lispbsd
dhcpcd=YES
sshd=NO
postfix=NO
inetd=NO
cron=NO
